[pytest]
# 구식 TemplateResponse(name, {...}) 시그니처가 다시 들어오면 경고가 아니라 실패로 잡는다.
# 예전에는 이 경고가 1,048건 쌓여 진짜 문제를 덮었고, starlette 를 올리면 동작이 깨진다.
filterwarnings =
    error::DeprecationWarning:starlette.templating
